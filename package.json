{
  "name": "grid",
  "version": "0.0.0",
  "description": "",
  "author": "",
  "keywords": [],
  "license": "ISC",
  "type": "module",
  "scripts": {
    "app": "pnpm --filter app",
    "build": "lerna run --scope \"@grid/*\" build",
    "prepare": "husky install",
    "lint": "echo \"Error: no linters\" && exit 1"
  },
  "engines": {
    "node": ">=18.16.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "lerna": "^8.1.2",
    "lint-staged": "^15.2.2"
  },
  "lint-staged": {
    "**/*.{js,ts}": [
      "lint"
    ]
  },
  "dependencies": {
    "@types/uuid": "^9.0.8",
    "husky": "^9.0.10",
    "uuid": "^9.0.1"
  }
}
